# Ativação do Supabase — appCasal

## Estado verificado

- O projeto PostgreSQL do Supabase foi criado na região South America (São Paulo).
- A URL pública e a chave publishable foram validadas por teste de conectividade sem expor valores.
- O editor SQL do projeto `appcasal` está acessível com o papel `postgres` selecionado.
- O usuário confirmou a aplicação das migrations `0001` a `0009` em ordem.

## Próxima ação

Aplicar as migrations privadas do appCasal no editor SQL e registrar o resultado de cada etapa antes de ativar dados reais.

## Verificação do editor

- O editor SQL do projeto correto está aberto e o usuário confirmou a execução das alterações estruturais.
- A interface ainda não expôs um modelo editável ao navegador; a aplicação da migration será retomada depois que o editor terminar a inicialização, sem executar comandos em uma tela não pronta.
- Após a navegação de diagnóstico, o editor SQL foi reaberto no projeto `appcasal` com o papel `postgres`; a próxima etapa é inserir a migration pelo controle de edição correto.
